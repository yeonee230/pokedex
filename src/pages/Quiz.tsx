import React from 'react';
import styled from 'styled-components'

export default function Quiz() {
    return (
        <QuizEl>
            Quiz
        </QuizEl>
    );
}

const QuizEl = styled.div``;