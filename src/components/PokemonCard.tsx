import React from 'react';
import styled from 'styled-components'

export default function PokemonCard() {
    return (
        <PokemonCardEl>
            PokemonCard
        </PokemonCardEl>
    );
}

const PokemonCardEl = styled.div``;